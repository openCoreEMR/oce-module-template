name: actionlint

on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/**
  pull_request:
    branches:
    - main
    paths:
    - .github/workflows/**

jobs:
  actionlint:
    uses: openCoreEMR/github-workflows-public/.github/workflows/actionlint.yml@0.0.4
