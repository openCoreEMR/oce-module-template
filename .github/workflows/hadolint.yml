name: hadolint

on:
  push:
    branches:
    - main
    paths:
    - '**/Dockerfile*'
    - .github/workflows/hadolint.yml
  pull_request:
    branches:
    - main

jobs:
  hadolint:
    uses: openCoreEMR/github-workflows-public/.github/workflows/hadolint.yml@1.0.0
